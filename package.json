{
  "name": "@dk/monorepo",
  "private": true,
  "version": "1.0.0",
  "comments": {
    "turbo-filter-template": "Exclude the template package from pipeline runs"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build --filter=!@dk/template --output-logs=errors-only",
    "lint": "turbo run lint --filter=!@dk/template --output-logs=errors-only"
  },
  "devDependencies": {
    "eslint": "8.37.0",
    "prettier": "2.8.7",
    "turbo": "1.9.3",
    "typescript": "5.0.3"
  }
}
